# Only needed to regenerate the .sqlx query cache (cargo sqlx prepare).
# Building and testing use the committed cache and need no database.
# The path must be absolute; sqlx does not expand ~ or resolve relative paths
# the way you would expect from the shell.
DATABASE_URL=sqlite:///absolute/path/to/nomanga/packages/services/nomanga-dev.db
