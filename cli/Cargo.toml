[package]
name = "nomanga-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
nomanga-core = { path = "../packages/core" }
nomanga-host = { path = "../packages/host" }

clap = { version = "4.5", features = ["derive"]}
serde = { workspace = true }
serde_json = { workspace = true }
